{
  "name": "superdoh-rs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-worker.cjs",
    "build:config": "node scripts/build-config.cjs",
    "predeploy": "npm run build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "4.118.0"
  }
}
